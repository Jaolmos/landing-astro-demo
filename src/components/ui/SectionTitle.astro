---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="section-title reveal">
  <h2>{title}</h2>
  {subtitle && <p>{subtitle}</p>}
</div>
